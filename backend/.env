# Postgres on Render / cloud
DATABASE_URL="postgresql://<USER>:<PASSWORD>@<HOST>:5432/<DBNAME>?schema=public"
# Render will set PORT automatically, keep fallback
PORT=4000

# Comma separated allowed origins (add your frontend production host)
FRONTEND_ORIGIN="https://your-frontend.onrender.com,http://localhost:3000"

